# Server
PORT=4000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/fmcg_scm?schema=public"

# Auth
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"

# Redis (optional, for production cache/queue - LRU cache works standalone without this)
REDIS_URL="redis://localhost:6379"
